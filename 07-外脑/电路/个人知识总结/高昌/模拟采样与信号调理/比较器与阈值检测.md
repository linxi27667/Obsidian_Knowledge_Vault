# 比较器与阈值检测

> 比较器判断“是否超过阈值”；ADC 加软件判断“当前具体是多少”。

- **ADC + 软件阈值**：灵活，适合欠压、过压、传感器报警等可调规则。
- **专用比较器**：快速输出 0/1，适合必须硬件立即保护的场合。

LM358 能处理简单比较，但不是为快速翻转优化。若需要明确阈值、快速响应或滞回，优先选 LM393 等比较器，并按输出结构配置上拉电阻。

关联：[[运算放大器]]、[[分压与ADC采样]]。
