# 分压与 ADC 采样

> 分压器把高电压缩小到 ADC 允许范围；ADC 再把连续电压转成数字量。

```text
V_ADC = V_IN × R下 / (R上 + R下)
```

例如 12V 经 `26.7kΩ` 与 `10kΩ` 分压约为 3.27V。设计时必须为输入过压、器件误差和浪涌留余量，不能只按“12V 标称值”计算。

ADC 采样时内部电容会短暂吸电；因此 ADC 前的小电阻、对地电容和电压跟随器都能提升稳定性。

关联：[[电压跟随器]]、[[RC低通滤波]]。
