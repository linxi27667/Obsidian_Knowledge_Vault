# 晶振、复位与启动

> 这三部分决定 MCU 是否按预期上电、运行和进入可调试状态。

- **8MHz HSE 晶振**：提供稳定时钟，可经 PLL 倍频；负载电容必须按晶振规格确认。
- **NRST 复位**：STM32 是低电平复位，典型做法为上拉、对地电容和可选按键。
- **BOOT0**：默认下拉，使 MCU 从内部 Flash 正常启动。
- **SWD**：保留 SWDIO、SWCLK、GND 与 3.3V 测试点，工业板不需要依赖 USB 自动下载。

关联：[[STM32最小系统]]、[[GPIO与接口规划]]。
